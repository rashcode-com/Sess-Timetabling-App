{
  "name": "sess-timetabling-app",
  "version": "0.1.5",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "web:serve": "pnpm --filter @sess/web serve",
    "web:build": "pnpm --filter @sess/web build",
    "start": "node server.js"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
